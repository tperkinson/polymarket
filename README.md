# Polymarket Workspace

This repository starts as a lightweight knowledge system for future conversations and tools.

Begin with:

```bash
./scripts/context_snapshot.sh
```

Primary context lives in `knowledge/START_HERE.md`.

